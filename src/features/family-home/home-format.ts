// STUB (red-test commit): replaced by the implementation commit.
export function shortDate(_input: string | Date): string {
  return "";
}

export function formatDollars(_amount: number): string {
  return "";
}
