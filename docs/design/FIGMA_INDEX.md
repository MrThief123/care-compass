# Figma index

Generated by F0-01 (17 Sep 2026) via Figma MCP `get_metadata` against file `DFcLy7U1caCVNlhT6eazF5`.

## Reachable via MCP

| Page | Node ID | Contents |
|---|---|---|
| 01 · Foundations | `0:1` | canvas |
| — Foundations · Colour | `3:2` | colour token swatches (bg/*, text/*, border/*) — see ARCHITECTURE.md §4 for the extracted hex values |
| — Foundations · Type, Space, Radius | `3:100` | type ramp (IBM Plex Sans), spacing scale (space/1–9), radius scale (pill/control/card/full), rail gradient note |

## Not reachable via MCP (still, as of 17 Sep 2026)

Pages 02–06 (screens, states) are **not** exposed by the Figma MCP connection — `get_metadata` on the file lists only the single top-level page `01 · Foundations` (`0:1`). This reproduces the exact gap recorded in DECISIONS.md OQ-19 at planning time; nothing has changed on the MCP side since.

Per PD (OQ-19, ANSWERED 2026-09-17): screens are built from the exported reference images in `docs/design/screens/` (see `docs/design/README.md` for the file → screen map) plus the Foundations tokens above, not from live Figma nodes. Any screen/control absent from both the images and the Foundations tokens is a design gap — flag it in the implementing feature's PR ("design gap, built from tokens — please review") per the OQ-19 answer; do not pause for a separate design step.

## Re-check procedure

If Figma access changes (e.g. pages 02–06 become visible), re-run `get_metadata` with no `nodeId` first to list top-level pages, then `get_metadata` with each page's node ID to enumerate frames, and update this file with the new node IDs before any feature starts consuming live Figma nodes instead of the exported images.
