# Session State — FAM-UI-07 Family Task log and Task detail screens (UI)

Last session date: 2026-09-20
Current branch: `feature/family-ui-task-log-detail` (parent `family-dev`; UI-04 `feature/shared-screen-contracts-fixtures` merged in, FD-14). Worktree: `/Users/dhruv/Documents/Dev/care-compass/.claude/worktrees/agent-a1cf1306432fc6abb`.
Worked on: Task A, the human's layout bug ("Things should fit into their tabs and if too big then should resize or get cutoff rather than overlap"): the Task log now uses a local fixed-track table (`task-log-table.tsx`, FD-20) that wraps or cuts text and re-flows into cards below a 48rem-wide log. Red tests 77af56f, green 573c732, both pushed.
Tests run: `npx vitest run src` 74 files / 747 tests pass; lint 0 errors / 23 baseline warnings; typecheck and format clean. Plain `npx vitest run` not re-run yet.
Current blocker: none. The PR must wait for UI-04 to reach `main` and be synced into `family-dev` (FD-14).
Important discoveries: the shared `DataTable` is an auto-layout table, so a 430px Done pill set every row's STATUS width; the kit `StatusPill` label already truncates but its icon shrinks in a squeezed pill (fixed with `[&>svg]:shrink-0`); Tailwind 4.3 container queries (`@container`, `@3xl:`) and arbitrary `[grid-template-areas:...]` work here; `getByText` on a link's inner span is what tests should use; clicking the title link stops the row click by design (tests click the date to test the row); the 375px page-level scroll (+57px) is the family layout header, not this feature.

## Exact next actions (in order; tests first, commit `test(family): ...` red, then implement, push after each)
Working rules: do not edit `src/server`, `src/mocks`, `src/types`, `src/lib`, `src/components/{ui,shared}` or the family `layout.tsx`; no AI attribution in commits; no PR; run one test file while working and the full suites at milestones.

**Real-browser sweep (how Task A was proved; repeat for B and C).** The dev server for this worktree is on http://localhost:3107 (`NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321 NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy SUPABASE_SERVICE_ROLE_KEY=dummy nohup npx next dev -p 3107 > /Users/dhruv/.claude/jobs/18b629ff/tmp/preview-tasks.log 2>&1 &` if it died; never start a second one, Next holds `.next/dev/lock`). Throwaway scripts (NOT in the repo, do not commit; recreate if missing) are in `/Users/dhruv/.claude/jobs/18b629ff/tmp/`: `sweep-log.mjs <path> [shotWidths]` (Playwright from this worktree's `node_modules/playwright-core`, Chromium 1194 cached; sets viewport 1920, 1440, 1280, 1024, 900, 768, 640, 375, then per row measures: text overlap between the four content boxes (a clipped element counts by its own box, because `Range.getClientRects` reports the full unclipped text), pill past the card edge, painted content outside the card, page-level horizontal scroll, tallest row, pill height; screenshots to `sweep-<w>.png`), `shot-row.mjs <path> <width> <rowText> <out.png> <height>` (scrolls a row into view and screenshots), `overflow.mjs <path> <width>` (lists elements past the viewport edge). Use `/family/client-margaret/tasks?as=family`. For the detail page use the URL-encoded key path `/family/client-margaret/tasks/event-margaret-eye-drops%3A2026-11-25T16%3A30%3A00%2B11%3A00?as=family` (51-character carer, 92-character document name) and `event-margaret-physio%3A2026-11-30T11%3A30%3A00%2B11%3A00` (2 documents). For 537/5,000-row pager checks in the browser use a throwaway route or fake data and delete it before committing (the volume fixtures in `fake-task-log.ts` work through `vi.mock` in tests).

1. **Task B, Task detail at narrow widths and the document tile** (`src/features/family-task-detail/`). Tile: min width about 160px in a wrapping grid (the `ul` is `flex flex-wrap gap-3` with a fixed `w-26`, about 104px, which breaks "Medication chart.pdf" into "Medication ch / art.pdf"); name `line-clamp-2` with `title`, no mid-word break for normal names, `overflow-wrap:anywhere` only as a last resort for a name with no spaces (the 92-character fixture name); type and size line on one line (`whitespace-nowrap`, truncate if needed). Detail page: header "Assigned to <long name>" wraps, Status card pill capped (`max-w-full`, `min-w-0`, label ellipsis, `[&>svg]:shrink-0`, full text in `title` and DOM), description with 2,000 characters and an unbroken 300-character string wraps (`whitespace-pre-line` plus `[overflow-wrap:anywhere]`), Back link stays. Tests: classes for each cause plus a 2,000-char and a 300-char-unbroken description in `task-detail-view.test.tsx`/`document-tile.test.tsx` (`[FAM-UI-07][PRD]`), run red, commit `test(family): ...`, implement, sweep the two detail URLs at all eight widths, record the table in PROGRESS.md, add a FD entry if a design call was made, commit and push.
2. **Task C, pager** (`task-log-pager.tsx`). With `makeHistory(537)` (27 pages) and `makeHistory(5000)` (250 pages): windowed with ellipses (already 7 slots), 44px targets (`h-11 min-w-11`), no overflow or overlap at 1024, 768, 640 and 375: the pager already wraps (`flex-wrap`); if page numbers still crowd at 375 use a compact Previous / 'Page x of y' / Next layout below a recorded container breakpoint (hide the number links with `hidden`, keep one structure for assistive technology, or drop the numbers below it and keep the `nav` named). Tests for the classes, `[FAM-UI-07][PRD]` or `[AC-05]`; browser check with a throwaway route or fake data (delete it before committing).
3. **Task D, visual polish (cap it at a few commits)**: FD-18/FD-09 item 5 differences via className overrides where testable (search border and placeholder colour, focus ring, table header colour and underline, row-separator inset, pill height 26px vs about 24px, the 4px gap under the Status label: `Field`/`SearchField` accept `className`, tailwind-merge lets `gap-2` beat `gap-1`), the kit's 24px Clear-search button (44px rule) via a local override or a recorded follow-up, and an accessible name for the search input (`SearchField` has no label prop; try `aria-label` through an allowed prop or a wrapper; otherwise write down exactly why not). Anything needing a shared change becomes a recorded follow-up request in DECISIONS.md, not an edit. Compare with `docs/design/screens/family-07-task-log.png` and `family-08-task-detail.png`.
4. **Task E, record and finish**: (1) DECISIONS.md FD-21 (next free number), quoting the human on 2026-09-20: FD-04 (Home design shows 3 overdue vs Task log 2) "Doesn't matter", so the Task log's reading (2) stays; FD-05 (within-day order) "Based off time they were created in calendar", read as strictly by the calendar start time, i.e. the contract rule as built (newest first, ties by key ascending; Mon 30 Nov reads Afternoon check-in, Physiotherapy, Morning medication), so AC-01's wording stands; mark both ANSWERED and update the AC-01 and HUMAN REVIEW notes in PROGRESS.md; (2) the shared `DataTable` follow-up is recorded in FD-20 (mention it in the PR body); (3) `git fetch origin` then merge `origin/feature/shared-screen-contracts-fixtures` once (it adds only docs commit 788cd6c) and keep the PR order note (UI-04 -> main -> sync family-dev -> this PR, FD-14); (4) update ACCEPTANCE_CRITERIA, TEST_PLAN, PROGRESS, SESSION_STATE, DECISIONS honestly; for any existing test whose expectation changed record test ID, before, after, reason in DECISIONS.md and flag "HUMAN REVIEW: test expectation changed" in PROGRESS.md (none so far in Tasks A); (5) final verification, one at a time: `npx vitest run src`, `npm run lint`, `npm run typecheck`, `npm run format:check`, plain `npx vitest run` (only `tests/integration/shared-supabase-environment.test.ts` may fail: it crashes at import without Supabase env vars); known flakes not ours: `tests/integration/mocks-import-boundary.test.ts` (5 s timeout under load, passes alone) and `day-timeline.test.tsx` (2 tests near an hour boundary); (6) END SESSION and report READY FOR PR (no PR).
Files likely to be touched next: `src/features/family-task-detail/document-tile.tsx`, `task-detail-view.tsx` (+ tests), `src/features/family-task-log/task-log-pager.tsx` (+ test), `src/features/family-task-log/task-log-view.tsx`, the docs above.
Preview URLs (dev server on :3107): `/family/client-margaret/tasks?as=family`, `...?as=family&page=7`, `...?as=family&status=overdue`, and the two detail paths above.
